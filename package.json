{
  "name": "my-nearby-worker",  # <--- Use your actual worker name here
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild worker.js --bundle --outfile=dist/worker.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.12"
  }
}
